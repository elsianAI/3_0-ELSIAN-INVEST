# Python dependencies for 3_0-ELSIAN-INVEST ENGINE

# JSON schema validation (optional but recommended)
jsonschema>=4.20.0
pypdf>=4.0.0

# No other dependencies required - engine is pure Python 3.9+
# LLM backends are external CLI tools (codex, gemini, claude)
